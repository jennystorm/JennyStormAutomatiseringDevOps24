---
- name: Uninstall all our favorite software
  hosts: all
  tasks:
    - name: Uninstall vim, bash and qemu
      become: true
      ansible.builtin.package:
        name: vim,bash-completion,qemu-guest-agent
        state: absent
